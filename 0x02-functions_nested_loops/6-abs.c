#include "main.h"
/**
  *_abs - compute the absolute of a integer
  *@int: The variable xe find absolute
  *Return: return 0
  */
int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (-1 * n);
	return (0);
}
